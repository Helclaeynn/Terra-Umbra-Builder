import fs from 'node:fs';
import path from 'node:path';
import zlib from 'node:zlib';

const ROOT='character-builder/rulesets/terra-umbra/reality';
const SAFE=path.join(ROOT,'safe');
const CANDIDATES='compendium/source/legacy-equipment-candidates-v1.json';
const OUT='compendium/source/legacy-equipment-audit-v1.json';
const SUMMARY_OUT='compendium/source/legacy-equipment-audit-summary-v1.json';
const MISSING_OUT='compendium/source/legacy-equipment-missing-v1.json';
const LIKELY_OUT='compendium/source/legacy-equipment-likely-v1.json';
const SCHEMA_OUT='compendium/source/legacy-equipment-schema-samples-v1.json';
const NAME_KEYS=['name','nom','equipement','equipment','item','designation'];
const CATEGORY_KEYS=['category','categorie','catégorie','section','family','famille','type','groupe'];

function norm(v){return String(v??'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9]+/g,' ').trim();}
function field(obj,names){const wanted=new Set(names.map(norm));for(const [k,v] of Object.entries(obj||{})){if(wanted.has(norm(k))&&v!==null&&v!==undefined&&String(v).trim())return v;}return null;}
function collect(node,pathParts=[],out=[]){if(Array.isArray(node)){node.forEach((v,i)=>collect(v,pathParts.concat(i),out));return out;}if(!node||typeof node!=='object')return out;const keys=Object.keys(node).map(norm);const hasName=keys.some(k=>NAME_KEYS.map(norm).includes(k));if(hasName){const name=field(node,NAME_KEYS);if(name)out.push({name:String(name).trim(),category:String(field(node,CATEGORY_KEYS)||pathParts.join(' / ')).trim(),raw:node,path:pathParts});}for(const [k,v] of Object.entries(node))if(v&&typeof v==='object')collect(v,pathParts.concat(k),out);return out;}
function cleanB64(s){return String(s).replace(/^\uFEFF/,'').replace(/\s+/g,'');}
function loadEquipment(){const m=JSON.parse(fs.readFileSync(path.join(SAFE,'equipment.manifest.json'),'utf8'));const b64=m.chunks.map(f=>cleanB64(fs.readFileSync(path.join(SAFE,f),'utf8'))).join('');return {manifest:m,data:JSON.parse(zlib.gunzipSync(Buffer.from(b64,'base64')).toString('utf8'))};}
function stripLegacyPrefix(name){return norm(name).replace(/^(pw|p|cos|bk|bh|wm|gl|b|cb|esw|wsg|ar|sr|dgr|dg|lmg|mg|pp|hp|lp|spc|sba|bpv|lba|hba)\s+/,'').trim();}
function tokens(s){return new Set(norm(s).split(/\s+/).filter(x=>x.length>1));}
function score(a,b){const A=tokens(a),B=tokens(b);if(!A.size||!B.size)return 0;let inter=0;for(const t of A)if(B.has(t))inter++;return 2*inter/(A.size+B.size);}
function shallowRaw(raw){const out={};for(const [k,v] of Object.entries(raw||{})){if(v===null||['string','number','boolean'].includes(typeof v))out[k]=v;else if(Array.isArray(v)&&v.every(x=>x===null||['string','number','boolean'].includes(typeof x)))out[k]=v;}return out;}

const source=JSON.parse(fs.readFileSync(CANDIDATES,'utf8'));const loaded=loadEquipment();const rows=collect(loaded.data);const unique=[];const seen=new Set();for(const row of rows){const k=norm(`${row.name}|${row.category}`);if(seen.has(k))continue;seen.add(k);unique.push(row);}const currentByName=new Map(unique.map(x=>[norm(x.name),x]));const aliases=source.rules?.confirmedAliases||{};const results=[];
for(const [document,names] of Object.entries(source.documents||{}))for(const legacyName of names){const alias=aliases[legacyName]||null;const exact=currentByName.get(norm(legacyName))||null;const aliasHit=alias?currentByName.get(norm(alias))||unique.find(x=>norm(x.name).endsWith(norm(alias).split(' ').slice(-1)[0])):null;const needle=stripLegacyPrefix(legacyName);const titleHits=unique.filter(x=>{const n=norm(x.name);return n===needle||n.endsWith(' '+needle)||n.includes(' '+needle+' ');});const candidates=unique.map(x=>({name:x.name,category:x.category,score:Number(Math.max(score(needle,x.name),score(legacyName,x.name)).toFixed(3))})).filter(x=>x.score>0).sort((a,b)=>b.score-a.score||a.name.localeCompare(b.name)).slice(0,5);let status='missing';let matchedRow=null;let confidence='none';if(exact){status='existing';matchedRow=exact;confidence='exact';}else if(aliasHit){status='existing';matchedRow=aliasHit;confidence='confirmed-alias';}else if(titleHits.length===1){status='likely-existing';matchedRow=titleHits[0];confidence='title';}else if(candidates[0]?.score>=0.74&&(!candidates[1]||candidates[0].score-candidates[1].score>=0.12)){status='likely-existing';matchedRow=currentByName.get(norm(candidates[0].name))||unique.find(x=>x.name===candidates[0].name);confidence='fuzzy';}const match=matchedRow?{name:matchedRow.name,category:matchedRow.category}:null;results.push({document,legacyName,status,confidence,confirmedAlias:alias,match,candidates,matchedRaw:matchedRow?shallowRaw(matchedRow.raw):null});}
const summary={total:results.length,existing:results.filter(x=>x.status==='existing').length,likelyExisting:results.filter(x=>x.status==='likely-existing').length,missing:results.filter(x=>x.status==='missing').length};const report={version:1,builderManifest:loaded.manifest,currentUniqueEntries:unique.length,currentEntries:unique.map(({name,category})=>({name,category})),summary,results};fs.writeFileSync(OUT,JSON.stringify(report,null,2)+'\n');
const rootShape=Array.isArray(loaded.data)?{type:'array',length:loaded.data.length}:{type:'object',keys:Object.keys(loaded.data||{})};const sampleNames=['Phoenix AR-124 Mutilator','Owl SR-017 Big Game Hunter','Owl LC-014 Chasseur','Raven Black Feathers','BridgeElectrics No-Fire'];const samples=sampleNames.map(name=>{const row=currentByName.get(norm(name));return row?{name:row.name,category:row.category,path:row.path,raw:row.raw}:null;}).filter(Boolean);const existing=results.filter(x=>x.status==='existing').map(({document,legacyName,confidence,match,confirmedAlias,matchedRaw})=>({document,legacyName,confidence,match,confirmedAlias,matchedRaw}));const likely=results.filter(x=>x.status==='likely-existing').map(({document,legacyName,confidence,match,candidates,matchedRaw})=>({document,legacyName,confidence,match,candidates,matchedRaw}));const missing=results.filter(x=>x.status==='missing').map(({document,legacyName,candidates})=>({document,legacyName,candidates}));const compact={version:1,builderManifest:loaded.manifest,catalogShape:rootShape,currentUniqueEntries:unique.length,summary,existing,likelyExisting:likely,missing,samples};fs.writeFileSync(SUMMARY_OUT,JSON.stringify(compact,null,2)+'\n');fs.writeFileSync(MISSING_OUT,JSON.stringify({version:1,count:missing.length,entries:missing},null,2)+'\n');fs.writeFileSync(LIKELY_OUT,JSON.stringify({version:1,count:likely.length,entries:likely},null,2)+'\n');fs.writeFileSync(SCHEMA_OUT,JSON.stringify({version:1,catalogShape:rootShape,samples},null,2)+'\n');console.log(`Legacy audit: ${summary.total} candidates · ${summary.existing} existing · ${summary.likelyExisting} likely · ${summary.missing} missing.`);