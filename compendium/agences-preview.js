const $=s=>document.querySelector(s);const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));let DB=null;const slugFromHash=()=>decodeURIComponent(location.hash.replace(/^#\/?/,''))||'agences-californiennes';
async function decodePreview(){const b64=window.AGENCES_PREVIEW_GZ||'';if(!b64)throw new Error('Paquet de données absent.');if(typeof DecompressionStream==='undefined')throw new Error('Ce navigateur ne prend pas en charge DecompressionStream.');const raw=Uint8Array.from(atob(b64),c=>c.charCodeAt(0));const stream=new Blob([raw]).stream().pipeThrough(new DecompressionStream('gzip'));const text=await new Response(stream).text();const data=JSON.parse(text);if(!Array.isArray(data.pages)||data.pages.length!==18)throw new Error(`Contrôle de recette échoué : ${data.pages?.length||0} pages au lieu de 18.`);return data}
function renderNav(){const nav=$('#nav');nav.innerHTML='';let g='';for(const p of DB.pages){if(p.group!==g){g=p.group;const h=document.createElement('div');h.className='nav-group';h.textContent=g;nav.appendChild(h)}const a=document.createElement('a');a.href='#/'+encodeURIComponent(p.id);a.dataset.id=p.id;a.innerHTML=`<span>${esc(p.title)}</span>`;nav.appendChild(a)}}
function render(){const id=slugFromHash();const p=DB.pages.find(x=>x.id===id)||DB.pages[0];document.title=`${p.title} · Preview TUC`;document.querySelectorAll('#nav a').forEach(a=>a.classList.toggle('active',a.dataset.id===p.id));const flags=(p.flags||[]).map(x=>`<div class="flag-box"><strong>À statuer avant intégration</strong><br>${esc(x)}</div>`).join('');const canon=(p.canon||[]).map(x=>`<div class="canon-box"><strong>Arbitrage / confirmation récente</strong><br>${esc(x)}</div>`).join('');const visuals=p.visuals?.length?`<div class="preview-visuals${p.visualKind==='map'?' maps':''}">${p.visuals.map((v,i)=>`<img loading="lazy" src="${esc(v)}" alt="Illustration source ${i+1} — ${esc(p.title)}">`).join('')}</div>`:'';const scheme=p.structure?.length?`<section class="scheme"><h2>Structure · synthèse documentaire</h2><div class="scheme-flow">${p.structure.map(n=>`<div class="scheme-node level-${n.level??1}"><strong>${esc(n.label)}</strong><span>${esc(n.note||'')}</span></div>`).join('')}</div></section>`:'';const sections=(p.sections||[]).map(s=>`<section class="preview-section section"><h2>${esc(s.title)}</h2>${(s.blocks||[]).map(b=>`<p class="body-p${b.type==='list'?' list':''}">${esc(b.text)}</p>`).join('')}</section>`).join('');$('#content').innerHTML=`<header class="page-head preview-hero"><div class="eyebrow">${esc(p.group)} · preview recette</div><h1>${esc(p.title)}</h1><p>${esc(p.summary)}</p><div class="preview-meta"><span class="badge source">${esc(p.source)}</span><span class="badge">${esc(p.target)}</span><span class="badge canon">Lore non-personnage</span></div></header>${visuals}${canon}${flags}${scheme}${sections}<div class="preview-foot">Source principale : ${esc(DB.source)} · Arbitrage : ${esc(DB.arbitration)} · ${DB.excludedProfiles} profils individuels détectés et volontairement exclus du chantier Lore.</div>`;window.scrollTo({top:0,behavior:'instant'})}
function applyFilter(){const q=$('#filter').value.trim().toLowerCase();document.querySelectorAll('#nav a').forEach(a=>{const p=DB.pages.find(x=>x.id===a.dataset.id);a.classList.toggle('hidden-nav',!!q&&!`${p.title} ${p.group} ${p.summary}`.toLowerCase().includes(q))})}
async function init(){try{DB=await decodePreview();renderNav();render();$('#filter').addEventListener('input',applyFilter);window.addEventListener('hashchange',render)}catch(err){console.error(err);$('#content').innerHTML=`<div class="notice"><strong>Preview invalide.</strong><br>${esc(err.message||err)}</div>`}}
init();
