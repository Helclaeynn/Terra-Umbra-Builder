let DB=window.PEGRE_PREVIEW_DB||null;const $=s=>document.querySelector(s);const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const slugFromHash=()=>decodeURIComponent(location.hash.replace(/^#\/?/,''))||'pegre-californienne';
function renderNav(){const nav=$('#nav');nav.innerHTML='';let g='';for(const p of DB.pages){if(p.group!==g){g=p.group;const h=document.createElement('div');h.className='nav-group';h.textContent=g;nav.appendChild(h)}const a=document.createElement('a');a.href='#/'+encodeURIComponent(p.id);a.dataset.id=p.id;a.innerHTML=`<span>${esc(p.title)}</span>`;nav.appendChild(a)}}
function render(){let id=slugFromHash();let p=DB.pages.find(x=>x.id===id)||DB.pages[0];document.title=`${p.title} · Preview TUC`;document.querySelectorAll('#nav a').forEach(a=>a.classList.toggle('active',a.dataset.id===p.id));const hasVisual=p.visuals?.length;const assetSrc=v=>(window.PEGRE_PREVIEW_ASSETS&&window.PEGRE_PREVIEW_ASSETS[v])||v;const visuals=hasVisual?`<div class="preview-visuals">${p.visuals.map((v,i)=>`<img loading="lazy" src="${esc(assetSrc(v))}" alt="Illustration source ${i+1} — ${esc(p.title)}">`).join('')}</div>`:'';const flags=(p.flags||[]).map(x=>`<div class="flag-box"><strong>À statuer avant intégration</strong><br>${esc(x)}</div>`).join('');const scheme=p.structure?.length?`<section class="scheme"><h2>Schéma de structure · synthèse de la source</h2><div class="scheme-flow">${p.structure.map((n,i)=>`<div class="scheme-node"><strong>${i+1}. ${esc(n.label)}</strong><span>${esc(n.note)}</span></div>`).join('')}</div></section>`:'';const sections=p.sections.map(s=>`<section class="preview-section section"><h2>${esc(s.title)}</h2>${s.blocks.map(b=>`<p class="body-p${b.type==='list'?' list':''}">${esc(b.text)}</p>`).join('')}</section>`).join('');$('#content').innerHTML=`<header class="page-head preview-hero${hasVisual?'':' no-visual'}"><div><div class="eyebrow">${esc(p.group)} · preview recette</div><h1>${esc(p.title)}</h1><p>${esc(p.summary)}</p><div class="preview-meta"><span class="badge source">${esc(p.source)}</span><span class="badge">${esc(p.target)}</span><span class="badge canon">Lore non-personnage</span></div></div>${visuals}</header>${flags}${scheme}${sections}<div class="preview-foot">Source de travail : ${esc(DB.source)} · Les biographies/profils individuels ont été exclus de cette preview. Les illustrations affichées sont extraites du document source et optimisées pour GitHub Pages.</div>`;window.scrollTo({top:0,behavior:'instant'})}
function applyFilter(){const q=$('#filter').value.trim().toLowerCase();document.querySelectorAll('#nav a').forEach(a=>{const p=DB.pages.find(x=>x.id===a.dataset.id);a.classList.toggle('hidden-nav',!!q&&!`${p.title} ${p.group} ${p.summary}`.toLowerCase().includes(q))})}
if(DB&&DB.pages&&DB.pages.length){renderNav();render();$('#filter').addEventListener('input',applyFilter);window.addEventListener('hashchange',render)}else{$('#content').innerHTML='<div class="notice">Impossible de charger les données de preview.</div>';}
