function stepSphere(){const c=wrap('Sphère actuelle & Style','Votre appartenance actuelle donne 5 points fixes ; le Style guide 5 points supplémentaires.');const spheres=Object.entries(ruleset.spheres).map(([id,s])=>({id,name:s.name,description:s.support}));c.appendChild(selectCards(spheres,state.creation.sphere,id=>{state.creation.sphere=id;state.creation.style='';state.talents.sphere='';state.talents.expertise='';Object.values(state.skills).forEach(v=>v.style=0);render()}));if(state.creation.sphere){c.appendChild(el('h3',{class:'subhead'},'Style'));const arr=ruleset.styles.filter(s=>s.sphere===state.creation.sphere).map(s=>({...s,description:`${s.lifestyle} · Compte ${fmt(s.account)} · Aug. ${fmt(s.augmentationEnvelope)}${s.vehicleCapital?` · Véhicule ${fmt(s.vehicleCapital)}`:''}`}));c.appendChild(selectCards(arr,state.creation.style,id=>{state.creation.style=id;state.talents.expertise='';Object.values(state.skills).forEach(v=>v.style=0);render()}));if(style())c.appendChild(el('div',{class:'rulebox'},`<strong>Compétences du Style :</strong> ${style().skills.map(skillName).join(' • ')}<br><strong>Expertise :</strong> ${style().expertiseFamilies.map(attrName).join(' / ')}<br><strong>Appui de Sphère :</strong> ${sphere().support}`))}}
function stepAttributes(){const ok=attrTotal()===attrBudget();const c=wrap('Attributs','Répartir le budget entre les cinq Attributs.',`<div class="kpi ${ok?'good':'bad'}">${attrTotal()} / ${attrBudget()}</div>`);const grid=el('div',{class:'alloc-grid'});ruleset.attributes.forEach(a=>{const v=state.attributes[a.id];const box=el('div',{class:'alloc'},`<div class="alloc-title"><strong>${a.name}</strong><span>${v}</span></div><div class="stepper"><button data-dir="-1">−</button><strong>${v}</strong><button data-dir="1">+</button></div>`);box.querySelectorAll('button').forEach(b=>b.onclick=()=>{const nv=v+Number(b.dataset.dir);if(nv>=3&&nv<=7){state.attributes[a.id]=nv;render()}});grid.appendChild(box)});c.appendChild(grid);c.appendChild(el('div',{class:'rulebox'},'Base : 22 points, minimum 3, maximum 7. L’achat Edge « +2 Attributs » augmente le budget à 24 sans relever le plafond.'))}
function stepSkills(){const st=style();const c=wrap('Compétences','5 points fixes de Sphère + 5 guidés de Style + points libres.',`<div><span class="kpi ${totalStylePoints()===5?'good':'bad'}">Style ${totalStylePoints()}/5</span> <span class="kpi ${totalFreePoints()===skillFreeBudget()?'good':'bad'}">Libres ${totalFreePoints()}/${skillFreeBudget()}</span></div>`);if(!st){c.appendChild(el('div',{class:'empty'},'Choisissez d’abord une Sphère et un Style.'));return}ruleset.attributes.forEach(a=>{c.appendChild(el('h3',{class:'subhead'},a.name));const grid=el('div',{class:'alloc-grid'});ruleset.skills.filter(s=>s.attribute===a.id).forEach(sk=>{const data=state.skills[sk.id],raw=skillRaw(sk.id),fixed=sphereFixed(sk.id),eligible=st.skills.includes(sk.id);const box=el('div',{class:'alloc'},`<div class="alloc-title"><strong>${sk.name}</strong><span>Brut ${raw}/5</span></div><div class="skill-meta">Sphère ${fixed} · Style ${data.style} · Libre/Edge ${data.free}</div><div class="inline" style="margin-top:8px"><span class="tag">Style</span><div class="stepper"><button data-kind="style" data-dir="-1">−</button><strong>${data.style}</strong><button data-kind="style" data-dir="1">+</button></div></div><div class="inline" style="margin-top:7px"><span class="tag">Libre</span><div class="stepper"><button data-kind="free" data-dir="-1">−</button><strong>${data.free}</strong><button data-kind="free" data-dir="1">+</button></div></div>${eligible?'<div class="skill-meta">Compétence de Style</div>':''}`);box.querySelectorAll('button').forEach(b=>b.onclick=()=>{const kind=b.dataset.kind,dir=Number(b.dataset.dir);if(kind==='style'){if(!eligible)return;const nv=data.style+dir;if(nv<0||nv>2)return;if(totalStylePoints()+dir<0||totalStylePoints()+dir>5)return;if(raw+dir>5)return;data.style=nv}else{const nv=data.free+dir;if(nv<0)return;if(totalFreePoints()+dir<0||totalFreePoints()+dir>skillFreeBudget())return;if(raw+dir>5)return;data.free=nv}render()});grid.appendChild(box)});c.appendChild(grid)});}
function talentRadio(label,items,value,onchange){const f=el('div',{class:'field'},`<label>${label}</label>`);const s=makeSelect(items,value,onchange);f.appendChild(s);const t=items.find(x=>x.id===value);if(t)f.appendChild(el('div',{class:'rulebox'},`<strong>${t.name}</strong><br>${t.effect}`));return f}
function accessibleEdgeTalents(){const arr=[...ruleset.talents.common,...ruleset.talents.expertise];if(state.creation.origin)arr.push(...ruleset.talents.origin[state.creation.origin]);if(state.creation.sphere)arr.push(...ruleset.talents.sphere[state.creation.sphere]);return arr.filter((t,i,a)=>a.findIndex(x=>x.id===t.id)===i)}
function stepTalents(){const c=wrap('Talents de Réalité','Quatre Talents gratuits, puis jusqu’à deux Talents supplémentaires via Edge.');if(!state.creation.origin||!state.creation.sphere||!style()){c.appendChild(el('div',{class:'empty'},'Choisissez Origine, Sphère et Style avant les Talents.'));return}const g=el('div',{class:'grid2'});g.appendChild(talentRadio('Talent d’Origine',ruleset.talents.origin[state.creation.origin],state.talents.origin,v=>{state.talents.origin=v;render()}));g.appendChild(talentRadio('Talent de Sphère',ruleset.talents.sphere[state.creation.sphere],state.talents.sphere,v=>{state.talents.sphere=v;render()}));const exp=ruleset.talents.expertise.filter(t=>style().expertiseFamilies.includes(t.attribute));g.appendChild(talentRadio('Expertise gratuite du Style',exp,state.talents.expertise,v=>{state.talents.expertise=v;render()}));g.appendChild(talentRadio('Talent commun',ruleset.talents.common,state.talents.common,v=>{state.talents.common=v;render()}));c.appendChild(g);c.appendChild(el('h3',{class:'subhead'},`Talents achetés avec Edge (${state.talents.edge.length}/${state.edge.talentPacks})`));for(let i=0;i<state.edge.talentPacks;i++){const f=el('div',{class:'field'},`<label>Talent Edge ${i+1}</label>`);const s=makeSelect(accessibleEdgeTalents().filter(t=>![state.talents.origin,state.talents.sphere,state.talents.expertise,state.talents.common,...state.talents.edge.filter((_,j)=>j!==i)].includes(t.id)),state.talents.edge[i]||'',v=>{state.talents.edge[i]=v;render()});f.appendChild(s);c.appendChild(f)}if(state.edge.talentPacks===0)c.appendChild(el('div',{class:'empty'},'Aucun achat de Talent par Edge. Cette dépense se règle à l’étape Edge.'));if(hasTalent('neurodriver')&&skillRaw('neurodive')<1)c.appendChild(el('div',{class:'rulebox bad'},'Neurodriver exige Neurodive 1+.'))}
function stepTruth(){
  sanitizeTruthTalents();
  const n=truthNature(),spent=truthPtvSpent(),remaining=truthPtvRemaining(),available=truthAvailableTalents();
  const c=wrap('Nature & Vérité','Choisissez ce que le personnage est, ce qu’il sait, puis achetez réellement ses capacités avec les 5 PTV de création.',`<div class="kpi ${remaining>=0?'good':'bad'}">PTV ${spent}/${ruleset.truth.ptvInitial}</div>`);
  const top=el('div',{class:'grid3'});
  [['Nature',ruleset.truth.natures,'nature'],['Conscience',ruleset.truth.consciousness,'consciousness']].forEach(([lab,opts,key])=>{
    const f=el('div',{class:'field'},`<label>${lab}</label>`);
    f.appendChild(makeSelect(opts,state.truth[key],v=>{
      if(key==='nature'){state.truth.nature=v;state.truth.choices={};state.truth.truthTalents=[];const nn=truthNature();(nn.choices||[]).forEach(ch=>{if(ch.optional&&ch.options?.some(o=>o.id==='aucune'))state.truth.choices[ch.key]='aucune'})}
      else state.truth[key]=v;render()
    }));
