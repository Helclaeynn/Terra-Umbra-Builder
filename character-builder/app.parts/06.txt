function stepEquipment(){const eco=economic();const c=wrap('Équipement & augmentations','Le catalogue détaillé viendra ensuite ; les budgets et règles économiques sont déjà actifs.',`<div class="kpi ${eco.account>=0?'good':'bad'}">Compte ${fmt(eco.account)}</div>`);if(!style()){c.appendChild(el('div',{class:'empty'},'Choisissez un Style pour obtenir les budgets.'));return}c.appendChild(el('div',{class:'grid3'},`<div class="rulebox"><strong>Compte de base</strong><br>${fmt(style().account)}<br><span class="muted">+ achats Edge et conversions</span></div><div class="rulebox"><strong>Enveloppe augmentique</strong><br>${fmt(eco.envelope)}<br><span class="muted">Gen2 : ${eco.gen2} fenêtre(s)</span></div><div class="rulebox"><strong>Capital véhicule</strong><br>${fmt(eco.vehicleCapital)}<br><span class="muted">Gundriver uniquement</span></div>`));const g=el('div',{class:'grid3'});[['augmentations','Dépenses augmentations'],['equipment','Dépenses équipement'],['vehicle','Dépenses véhicule']].forEach(([k,l])=>{const f=el('div',{class:'field'},`<label>${l}</label>`);const inp=el('input',{type:'number',min:0,value:state.spending[k]});inp.oninput=e=>{state.spending[k]=Math.max(0,Number(e.target.value)||0);render()};f.appendChild(inp);g.appendChild(f)});c.appendChild(g);c.appendChild(el('div',{class:'rulebox'},`Enveloppe inutilisée : ${fmt(eco.augUnused||0)} → ${fmt((eco.augUnused||0)*.5)} au Compte. Capital véhicule inutilisé : ${fmt(eco.vehUnused||0)} → ${fmt((eco.vehUnused||0)*.5)} au Compte. Le Compte complète l’Enveloppe à 1:1.`))}
function stepFinish(){const v=validations(),errs=currentErrors(),d=derived(),eco=economic();const c=wrap('Finalisation','Vérification globale et profil dérivé.',`<div class="kpi ${v.finish?'good':'bad'}">${v.finish?'Fiche valide':'À corriger'}</div>`);if(errs.length){errs.forEach(x=>c.appendChild(el('div',{class:'alert'},x)))}else c.appendChild(el('div',{class:'rulebox'},'<strong>Validation :</strong> aucune erreur bloquante détectée par le builder.'));const table=el('table',{class:'compact-table'},`<thead><tr><th>Valeur</th><th>Total</th></tr></thead><tbody>
<tr><td>PV maximum</td><td>${d.pvMax}</td></tr><tr><td>Seuil de mort</td><td>${d.death}</td></tr><tr><td>Initiative</td><td>1d10e + ${d.initiative}</td></tr><tr><td>Défense passive</td><td>${d.passiveDefense}</td></tr><tr><td>Défense occulte passive</td><td>${d.occultDefense}</td></tr><tr><td>Déplacement</td><td>${d.movement} m / PA</td></tr><tr><td>Intégrité</td><td>${d.integrity}</td></tr><tr><td>Stress augmentique max</td><td>${d.augmentStressMax}</td></tr><tr><td>Mêlée</td><td>1d10e + ${d.melee}</td></tr><tr><td>Pugilat</td><td>1d10e + ${d.pugilat}</td></tr><tr><td>Tir</td><td>1d10e + ${d.shooting}</td></tr><tr><td>Neurodive</td><td>1d10e + ${d.neurodive}</td></tr></tbody>`);c.appendChild(table);c.appendChild(el('div',{class:'rulebox'},`<strong>Train de vie :</strong> ${lifestyleFinal()} · <strong>Renommée :</strong> ${renown()} · <strong>Compte final :</strong> ${fmt(eco.account)} · <strong>Edge restant :</strong> ${edgeRemaining()} · <strong>PTV en réserve :</strong> ${truthPtvRemaining()}`))}

function renderSummary(){const s=$('#summaryContent'),d=derived(),eco=economic(),v=validations();$('#validBadge').className=`badge ${v.finish?'good':'bad'}`;$('#validBadge').textContent=v.finish?'Valide':'À compléter';const st=style();s.innerHTML=`
<div class="summary-section"><h4>Personnage</h4><div class="summary-line"><span>Nom</span><span>${state.identity.name||'—'}</span></div><div class="summary-line"><span>Concept</span><span>${state.identity.concept||'—'}</span></div></div>
<div class="summary-section"><h4>Cadre social</h4><div class="summary-line"><span>Origine</span><span>${origin()?.name||'—'}</span></div><div class="summary-line"><span>Sphère</span><span>${sphere()?.name||'—'}</span></div><div class="summary-line"><span>Style</span><span>${st?.name||'—'}</span></div><div class="summary-line"><span>Train de vie</span><span>${st?lifestyleFinal():'—'}</span></div></div>
<div class="summary-section"><h4>Ressources</h4><div class="summary-line"><span>Edge</span><span>${edgeRemaining()} / ${edgeTotal()}</span></div><div class="summary-line"><span>Compte</span><span>${st?fmt(eco.account):'—'}</span></div><div class="summary-line"><span>PTV réserve</span><span>${truthPtvRemaining()}</span></div><div class="summary-line"><span>Renommée</span><span>${renown()}</span></div></div>
<div class="summary-section"><h4>Dérivés</h4><div class="summary-line"><span>PV</span><span>${d.pvMax}</span></div><div class="summary-line"><span>Init.</span><span>+${d.initiative}</span></div><div class="summary-line"><span>Déf.</span><span>${d.passiveDefense}</span></div><div class="summary-line"><span>Déf. occ.</span><span>${d.occultDefense}</span></div><div class="summary-line"><span>Intégrité</span><span>${d.integrity}</span></div></div>
<div class="summary-section"><h4>Vérité</h4><div class="summary-line"><span>Nature</span><span>${truthNature()?.name||'—'}</span></div><div class="summary-line"><span>Conscience</span><span>${ruleset.truth.consciousness.find(n=>n.id===state.truth.consciousness)?.name||'—'}</span></div><div class="summary-line"><span>PTV</span><span>${truthPtvSpent()} / ${ruleset.truth.ptvInitial}</span></div></div>`;
}

function buildExport(){return {...structuredClone(state),skills:Object.fromEntries(ruleset.skills.map(s=>[s.id,{raw:skillRaw(s.id),sphere:sphereFixed(s.id),style:state.skills[s.id].style,free:state.skills[s.id].free}])),derived:derived(),economy:{...economic(),lifestyle:lifestyleFinal(),renown:renown(),edgeTotal:edgeTotal(),edgeRemaining:edgeRemaining()},validation:validations()}}
function downloadJSON(){const data=JSON.stringify(buildExport(),null,2);const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([data],{type:'application/json'}));a.download=(state.identity.name?slugClient(state.identity.name):'personnage')+'.tuc.json';a.click();URL.revokeObjectURL(a.href)}
function slugClient(s){return s.normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'')}
function persist(){localStorage.setItem('tuc-character-builder-v1',JSON.stringify(state))}
function restore(){try{const raw=localStorage.getItem('tuc-character-builder-v1');if(!raw)return;mergeState(JSON.parse(raw))}catch(e){console.warn(e)}}
function mergeState(obj){
  for(const k of ['identity','creation','attributes','talents','edge','truth','social','spending']) if(obj[k]) Object.assign(state[k],obj[k]);
  if(obj.skills){ruleset.skills.forEach(s=>{if(obj.skills[s.id]){const x=obj.skills[s.id];state.skills[s.id]={style:Number(x.style||0),free:Number(x.free||0)}}})}
  if(Array.isArray(obj.disadvantages)) state.disadvantages=[...obj.disadvantages]; if(Array.isArray(obj.equipment))state.equipment=[...obj.equipment]; if(!state.truth.choices)state.truth.choices={}; if(!Array.isArray(state.truth.truthTalents))state.truth.truthTalents=[];
}

$('#prevBtn').onclick=()=>{if(stepIndex>0){stepIndex--;render()}};$('#nextBtn').onclick=()=>{if(stepIndex<STEPS.length-1){stepIndex++;render()}};
$('#saveBtn').onclick=()=>{persist();$('#saveBtn').textContent='Sauvegardé ✓';setTimeout(()=>$('#saveBtn').textContent='Sauvegarder',900)};
$('#exportBtn').onclick=downloadJSON;$('#loadBtn').onclick=()=>$('#fileInput').click();$('#fileInput').onchange=async e=>{const f=e.target.files[0];if(!f)return;try{mergeState(JSON.parse(await f.text()));render()}catch(err){alert('JSON invalide ou incompatible.')}e.target.value=''};
restore();render();
