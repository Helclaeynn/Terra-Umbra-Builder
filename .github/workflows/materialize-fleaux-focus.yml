name: Materialize fleaux focus lore

on:
  push:
    branches:
      - main
    paths:
      - 'compendium/source/fleaux-focus-2026-09-v1-*.jsonpart'
      - 'scripts/integrate-truth-lore-fleaux-focus-2026-09-v1.mjs'
      - 'compendium/tests/truth-lore-fleaux-focus-2026-09-check.mjs'
      - '.github/workflows/materialize-fleaux-focus.yml'

permissions:
  contents: write

jobs:
  materialize:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Materialize Fléaux focus corpus
        run: |
          node scripts/integrate-truth-lore-fleaux-focus-2026-09-v1.mjs
          node scripts/build-compendium-navigation.mjs
      - name: Validate Fléaux focus corpus
        run: |
          node compendium/tests/truth-lore-fleaux-focus-2026-09-check.mjs
          node compendium/tests/manifest-check.mjs
          node compendium/tests/truth-lore-corruption-fleaux-check.mjs
          node compendium/tests/wiki-context-audit.mjs
      - name: Commit materialized corpus
        shell: bash
        run: |
          set -euo pipefail
          if git diff --quiet -- compendium/data; then
            echo "No generated corpus changes."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A compendium/data
          git commit -m "feat(compendium): integrate fleaux focus lore"
          git push origin HEAD:main
